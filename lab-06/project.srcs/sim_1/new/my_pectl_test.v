`timescale 1ns / 1ps

module my_pectl_test();
    // TODO
    my_pectl UUT(
        .start(0),
        .reset(0),
        .clk(0),
        .rddata(0),
        .rdaddr(0),
        .out(0)
    );
endmodule
